{
    "checks": [
        { "when": "edit", "run": "node _tools/checks/run.mjs --paths {file}", "label": "checks that can read one file" },
        { "when": "turn", "run": "pnpm verify:turn", "label": "Verify before you finish" }
    ]
}
