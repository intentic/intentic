{
    "publisher": "intentic",
    "name": "preview",
    "version": "1.0.0",
    "category": "workspace",
    "engines": { "intentic": "^0.4.0" },
    "permissions": {
        "sandbox": ["GET /panels", "POST /panels/*/start", "POST /panels/*/stop", "GET /ports", "POST /ports/forward", "POST /ports/unforward"]
    },
    "contributes": {
        "views": [
            { "id": "preview", "label": "Preview", "surface": "directory" },
            { "id": "ports", "label": "Ports", "surface": "sandbox" }
        ]
    }
}
