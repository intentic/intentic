{
    "publisher": "intentic",
    "name": "telegram",
    "version": "1.0.0",
    "category": "connections",
    "logo": "telegram",
    "engines": {
        "intentic": "^2.0.0"
    },
    "contributes": {
        "listener": {
            "provider": "telegram",
            "events": [{ "type": "message", "label": "Messages" }],
            "automation": {
                "label": "Telegram",
                "mentionLabel": "Only when the bot is messaged directly (private chat, @mention or reply)",
                "channel": { "label": "Chat ID (optional)", "placeholder": "every chat the bot is in" },
                "starterPrompt": "A Telegram message just arrived — each line of the event payload is one JSON event of type `message`: `author` is who wrote it, `channelId` is the chat, `content` is the text (a `[voice note]`-style marker when they sent a file instead, with the file ids in `extra.attachments`), and `mentioned: true` means it was addressed to the bot. `history` holds the recent messages the gateway watched go by — a bot cannot read a chat's past, so that is all the context there is. Reply in plain text: your answer is streamed into the chat for you, and Telegram renders markdown literally, so no asterisks, headings or link syntax."
            }
        },
        "processes": [
            {
                "name": "gateway",
                "command": "node dist/gateway.js",
                "autoStart": true
            }
        ],
        "capabilities": [
            {
                "id": "telegram",
                "kind": "cli",
                "catalog": {
                    "name": "Telegram",
                    "logo": "telegram",
                    "description": "Read and reply in Telegram; wake on messages.",
                    "category": "communication",
                    "hint": "One token from @BotFather is the whole setup — no app review, no public URL. In GROUPS a bot sees only messages that @mention it until you turn privacy mode off (`/setprivacy` → Disable), which is the same trade-off as Discord's Message Content intent: hear everything, or only what is addressed to you.",
                    "guide": {
                        "url": "https://t.me/BotFather",
                        "linkLabel": "Open BotFather in Telegram",
                        "steps": [
                            "Message `@BotFather` and send `/newbot`, then pick a display name and a username ending in `bot`.",
                            "Copy the HTTP API token it hands back and paste it here.",
                            "For a group: add the bot to it, then `/setprivacy` → your bot → `Disable` if it should read every message rather than only @mentions.",
                            "Say hello to the bot in Telegram — it answers from the first message, no invite step."
                        ]
                    }
                },
                "fields": [
                    {
                        "key": "botToken",
                        "label": "Bot token",
                        "secret": true
                    }
                ],
                "env": {
                    "TELEGRAM_BOT_TOKEN": "${botToken}"
                },
                "skill": "skills/telegram/SKILL.md"
            }
        ]
    }
}
