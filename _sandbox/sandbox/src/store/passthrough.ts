// Carrying forward what a build does not know on its writes lives in the contract (@intentic/sandbox-contract/documents),
// shared with extensions' own stored files.
export { carryUnknown, type EntryRead, type IdKeys, readEntries, reemitQuarantined } from "@intentic/sandbox-contract/documents";
