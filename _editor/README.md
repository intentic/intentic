# The editor

The screen you actually look at — files, chat, terminals. The browser app, its thin native shells (desktop,
iOS, the Play-store TWA), the design system they draw with, and the UI kit extensions draw with. One product
codebase: every shell frames the same hosted web app rather than reimplementing any of it. Runs against the
platform for sign-in and against the sandbox daemon for everything else.
