# The editor

The screen you actually look at — files, chat, terminals. The browser app, its desktop shell, the design
system they draw with, and the UI kit extensions draw with. Runs against the platform for sign-in and against
the sandbox daemon for everything else.
