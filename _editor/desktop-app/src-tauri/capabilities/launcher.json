{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "launcher",
    "description": "The app's own face, local content, and the only window label with any IPC. The workspace face is remote content and gets none at all: its only channel into the app is the intentic:// navigation the window handler intercepts. `set-title` is here because this one window has two screens and its title says which is up (App.vue), which is also the only thing outside the process that can tell them apart.",
    "windows": ["launcher"],
    "permissions": ["core:default", "core:window:allow-set-title", "dialog:default"]
}
