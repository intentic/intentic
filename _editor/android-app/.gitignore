# Everything but the manifest is generated (bubblewrap init/update) or a signing secret.
# The manifest is the source of truth; `npx @bubblewrap/cli update` rebuilds the project from it.
app/
build/
gradle/
gradlew
gradlew.bat
*.gradle
gradle.properties
local.properties
store_icon.png
android.keystore
