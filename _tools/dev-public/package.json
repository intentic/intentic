{
    "name": "@intentic/dev-public",
    "version": "0.0.0",
    "description": "Serve a local dev platform on the internet through the tunnel hub — mint the grant, publish web + api, launch dev against the public origins",
    "license": "MIT",
    "private": true,
    "type": "module",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/intentic/intentic.git",
        "directory": "_tools/dev-public"
    },
    "files": [
        "src"
    ],
    "scripts": {
        "typecheck": "tsgo --noEmit -p tsconfig.test.json",
        "test": "vitest run"
    },
    "devDependencies": {
        "@intentic/fake-zrok": "workspace:*",
        "@intentic/testing": "workspace:*",
        "@intentic/tsconfig": "workspace:*",
        "@types/node": "catalog:",
        "@typescript/native-preview": "catalog:",
        "vitest": "catalog:"
    }
}
