# Generated by generate.mjs on install — this machine's own development CA and its localhost leaf. The private
# halves must never leave the machine that minted them; a committed CA is one anybody can sign with.
localhost-com-ca.crt
localhost-com-ca.key
localhost-com-ca.srl
localhost.crt
localhost.key
