# Generated by generate.mjs on install — this checkout's localhost leaf. The root that signs it is not here at
# all: it lives once per machine, in the OS's own per-user data directory, so that approving it once covers
# every checkout. A committed CA is one anybody can sign with, which is why neither half is in the repository.
localhost.crt
localhost.key
