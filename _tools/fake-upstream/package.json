{
    "name": "@intentic/fake-upstream",
    "version": "0.0.0",
    "description": "A stand-in for the model the free trial spends — Google's two surfaces, served locally and deterministically",
    "license": "MIT",
    "private": true,
    "type": "module",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/intentic/intentic.git",
        "directory": "_tools/fake-upstream"
    },
    "files": [
        "Dockerfile",
        "src"
    ],
    "exports": {
        ".": {
            "types": "./src/server.ts",
            "default": "./src/server.ts"
        }
    },
    "scripts": {
        "typecheck": "tsgo --noEmit -p tsconfig.test.json",
        "test": "vitest run"
    },
    "devDependencies": {
        "@intentic/testing": "workspace:*",
        "@intentic/tsconfig": "workspace:*",
        "@types/node": "catalog:",
        "@typescript/native-preview": "catalog:",
        "vitest": "catalog:"
    }
}
