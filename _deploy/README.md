# Deployment engine

A bundled tool that turns "what I want" into running servers — intent → needs → desired state → reconcile.
**Not product surface**: the sandbox can run it, one tool among many, and nothing in here may depend on the
app plane. Start at [ARCHITECTURE.md](../ARCHITECTURE.md)'s deployment sections or
[docs/deploy-engine.md](../docs/deploy-engine.md).
