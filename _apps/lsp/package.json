{
    "name": "@intentic/lsp",
    "version": "0.0.0",
    "description": "lsp — agent-native TypeScript rename + diagnostics CLI over the TS language service",
    "license": "MIT",
    "type": "module",
    "repository": {
        "type": "git",
        "url": "git+https://gitlab.com/radarsu/intentic.git",
        "directory": "_apps/lsp"
    },
    "files": [
        "dist"
    ],
    "bin": {
        "lsp": "./dist/cli.js"
    },
    "scripts": {
        "build": "tsc",
        "watch": "tsc --build --watch",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "tslib": "catalog:",
        "typescript": "catalog:"
    },
    "devDependencies": {
        "@intentic/tsconfig": "workspace:*",
        "@types/node": "catalog:",
        "vitest": "catalog:"
    }
}
