var e=(...e)=>{let t=e.join(`\0`),n=2166136261;for(let e=0;e<t.length;e++)n=((n^t.charCodeAt(e))>>>0)*16777619,n>>>=0;return n.toString(36).padStart(7,`0`)},t=e=>e<=0?-1:Math.floor(Math.log2(e)),n=({subject:e,why:t,diagnosis:n,goal:r,invariants:i,done:a})=>[e,`Why: ${t} ${n}`,r,`${i} ${a}`].join(`

`),r=`The measurement woke you; it did not decide anything. Read the repository before you touch it, and treat every finding as a claim to verify rather than a task to execute. If a finding is wrong, say why in one line and leave it — a run that verifies ten and fixes two is a good run.`,i=`Keep it mechanical and separately explainable: nothing lands that you could not justify on its own line of the summary. Do not reformat, rename or "while I was in here" anything the finding did not name. Run the repository's own type-check and tests before you finish, and if you cannot make them pass, leave the change out and say so.`,a=`Change nothing. This is a survey: the output is your findings, cited file:line, and a recommendation the owner can act on or dismiss. Where you would propose an edit, describe it and where it would go instead of making it.`,o=`Read it first. Behaviour stays identical, and the blast radius is this file, whatever it splits into, and the importers that must follow — no re-export shims left behind.`,s=[{id:`react`,label:`React`,packages:[`react`]},{id:`vue`,label:`Vue`,packages:[`vue`]},{id:`angular`,label:`Angular`,packages:[`@angular/core`]}],c=[`tailwindcss`],l=e=>s.filter(t=>t.packages.some(t=>e.includes(t))),u=e=>c.some(t=>e.includes(t)),ee=[`!**/node_modules/**`,`!**/dist/**`,`!**/build/**`,`!**/.next/**`,`!**/out/**`,`!**/coverage/**`,`!**/vendor/**`,`!**/generated/**`,`!**/*.{test,spec,stories}.*`],te=[`*.vue`,`*.tsx`,`*.jsx`,`*.component.ts`],d=[`*.vue`,`*.tsx`,`*.jsx`,`*.html`,`*.svelte`,`*.astro`],f=`-\\[(#[0-9a-fA-F]{3,8}|(rgb|hsl)a?\\(|[0-9]+(\\.[0-9]+)?px)`,p=[{id:`react-class-component`,framework:`react`,label:`class components`,replacement:`function components with hooks`,pattern:`extends\\s+(React\\.)?(Pure)?Component\\b`,globs:[`*.tsx`,`*.jsx`]},{id:`react-legacy-render`,framework:`react`,label:`the legacy ReactDOM.render entry point`,replacement:`createRoot from react-dom/client`,pattern:`ReactDOM\\.render\\(`,globs:[`*.tsx`,`*.jsx`,`*.ts`,`*.js`]},{id:`react-unsafe-lifecycle`,framework:`react`,label:`the pre-16.3 lifecycle methods`,replacement:`effects, or the UNSAFE_ prefixed names if the behaviour is genuinely wanted`,pattern:`\\bcomponentWill(Mount|ReceiveProps|Update)\\b`,globs:[`*.tsx`,`*.jsx`]},{id:`react-prop-types`,framework:`react`,label:`runtime prop-types`,replacement:`the component's own TypeScript props type`,pattern:`from\\s+[\\x22\\x27]prop-types[\\x22\\x27]`,globs:[`*.tsx`,`*.jsx`]},{id:`vue-options-api`,framework:`vue`,label:`the Options API`,replacement:`<script setup> with the Composition API`,pattern:`<script[^>]*\\bsetup\\b`,globs:[`*.vue`],absent:!0},{id:`vue-2-lifecycle`,framework:`vue`,label:`the Vue 2 teardown hooks`,replacement:`beforeUnmount and unmounted`,pattern:`\\b(beforeDestroy|destroyed)\\s*[(:]`,globs:[`*.vue`,`*.ts`,`*.js`]},{id:`vue-global-api`,framework:`vue`,label:`the Vue 2 global constructor`,replacement:`createApp and defineComponent`,pattern:`\\b(new\\s+Vue\\(|Vue\\.extend\\()`,globs:[`*.vue`,`*.ts`,`*.js`]},{id:`angular-ngmodule`,framework:`angular`,label:`NgModule declarations`,replacement:`standalone components`,pattern:`@NgModule\\(`,globs:[`*.ts`]},{id:`angular-structural-directives`,framework:`angular`,label:`the structural directives`,replacement:`the built-in control flow blocks`,pattern:`\\*ng(If|For|Switch)\\b`,globs:[`*.html`,`*.ts`]},{id:`angular-module-providers`,framework:`angular`,label:`the module-based providers`,replacement:`the provide* functions in the application config`,pattern:`\\b(HttpClientModule|BrowserAnimationsModule|RouterModule\\.forRoot)\\b`,globs:[`*.ts`]}],ne=e=>p.find(t=>t.id===e),m=e=>e.replace(/^\.\//,``),h=3,re=/^(base|the)/,ie=/(v[0-9]+|new|old|legacy|copy|component|[0-9]+)$/,ae=e=>{let t=((m(e).split(`/`).pop()??``).split(`.`)[0]??``).toLowerCase().replace(/[^a-z0-9]/g,``);if(t===``||t===`index`)return;let n=t.replace(ie,``),r=(n.length>=h?n:t).replace(re,``);return r.length>=h?r:t},g=864e5,_=`/tmp/intentic-chore-audit.json`,v=`/tmp/intentic-chore-knip.json`,y=`/tmp/intentic-chore-jscpd`,b=`${y}/jscpd-report.json`,x=e=>e===`root`||e===``?`the workspace root repository`:e,oe=e=>e===`root`||e===``?`workspace root`:e,S=(e,t,n=`${t}s`)=>`${e} ${e===1?t:n}`,se=e=>`${e.kind} · ${e.name} ${e.current} → ${e.latest}`,C=(e,t)=>{let n=e.probes.get(t);if(n?.state===`ok`&&n.facts!==void 0&&n.facts.id===t)return n.facts},ce=new Set([`critical`,`high`]),le={id:`security-advisories`,title:`Patch security advisories`,icon:`shield`,description:`Published advisories against this dependency tree, and the ones whose fix is a version bump.`,kind:`carrying`,criterion:`pnpm audit reports an advisory of high or critical severity against the resolved tree.`,applies:e=>e.shape.lockfile?void 0:`no lockfile`,stance:`act`,needs:[`audit`],cadenceMs:0,automation:{cron:`0 4 * * *`,guard:`pnpm audit --json > ${_} 2>/dev/null; [ "$(jq '(.metadata.vulnerabilities.high // 0) + (.metadata.vulnerabilities.critical // 0)' ${_} 2>/dev/null || echo 0)" -gt 0 ]`,note:`nightly · high + critical only`,report:_,woke:`pnpm audit's report for this workspace is in ${_} (JSON), and it woke you because it carries a high or critical advisory.`},assess:t=>{let n=C(t,`audit`);if(n===void 0)return;let r=n.advisories.filter(e=>ce.has(e.severity));if(r.length===0)return;let i=r.filter(e=>!e.dev),a=r.filter(e=>e.patched!==void 0);return{headline:`${S(r.length,`advisory`,`advisories`)}, ${a.length} with a published fix`,detail:r.toSorted((e,t)=>e.name.localeCompare(t.name)).map(e=>`${e.severity} · ${e.name} — ${e.title}${e.patched===void 0?` (no patch yet)`:``}`),digest:e(...r.map(e=>`${e.name}@${e.severity}`).toSorted()),severity:i.length>0?`warning`:`info`,why:`pnpm audit reports ${S(r.length,`high or critical advisory`,`high or critical advisories`)} against ${x(t.repo)} — ${i.length} reaching a production dependency path, ${a.length} with a published patched range: ${r.map(e=>`${e.name} (${e.severity}${e.dev?`, dev-only`:``}${e.patched===void 0?`, no patch`:`, fixed in ${e.patched}`})`).join(`; `)}.`}},diagnosis:`An advisory with a published fix is a version bump someone has to actually make; one without is a risk to decide about.`,goal:`For each advisory, establish whether this workspace reaches the vulnerable code path at all — a transitive dependency of a build-time tool is a different problem from one in a running service. Where the fix is a version bump the lockfile can absorb, make it. Where it needs a real upgrade or has no patch published, leave it and say what it would take. Never rewrite application code to route around a CVE.`,done:"Done when `pnpm audit` reports fewer high/critical advisories than it did, and the repository's type-check and tests pass."},ue=20,de={id:`dependencies-outdated`,title:`Update dependencies`,icon:`arrow-circle-up`,description:`How far behind the registry this tree has drifted, and which majors are waiting.`,kind:`accruing`,criterion:`A dependency is a major version behind, or more than 20 are behind by any amount.`,applies:e=>e.shape.packageManifest?void 0:`no package.json`,stance:`act`,needs:[`outdated`],cadenceMs:30*g,assess:n=>{let r=C(n,`outdated`);if(r===void 0)return;let i=r.packages.filter(e=>e.kind===`major`);if(!(i.length===0&&r.packages.length<ue))return{headline:i.length===0?`${S(r.packages.length,`package`)} behind`:`${S(i.length,`major`)} waiting, ${r.packages.length} behind in total`,detail:i.toSorted((e,t)=>e.name.localeCompare(t.name)).map(se),digest:e(...i.map(e=>`${e.name}@${e.latest}`).toSorted(),`total:${t(r.packages.length)}`),severity:`info`,why:`pnpm outdated reports ${S(r.packages.length,`dependency`,`dependencies`)} behind the registry in ${x(n.repo)}, ${i.length} of them by a major version${i.length===0?``:`: ${i.map(e=>`${e.name} ${e.current} → ${e.latest}`).join(`; `)}`}.`}},diagnosis:`Version drift is cheap to fix continuously and expensive to fix in one go, because the majors start depending on each other.`,goal:`Take the patch and minor upgrades in one pass — those are what the lockfile can absorb without argument. Then take the majors ONE AT A TIME, reading each one's changelog for breaking changes before you touch anything, and stop at the first one that needs more than a mechanical fix: leave it, and say what it would take. Do not batch majors; a failing test after eight of them is a bisect nobody wanted.`,done:`Done when the repository's type-check and tests pass, and your summary names every major you took and every one you left, with the reason.`},fe={id:`dead-code`,title:`Clear out dead code`,icon:`trash`,description:`Files, exports and dependencies nothing in this repository references any more.`,kind:`accruing`,criterion:`knip reports at least one unreferenced file, export or dependency.`,applies:e=>e.shape.packageManifest?void 0:`no package.json`,stance:`act`,needs:[`knip`],cadenceMs:14*g,automation:{cron:`0 3 * * *`,guard:`pnpm exec knip --version >/dev/null 2>&1 || { echo "knip is not a devDependency of this repo"; exit 1; }; pnpm exec knip --reporter json > ${v} && { echo "no dead code"; exit 1; }`,note:`nightly · wakes only on findings`,report:v,woke:`knip's findings for this workspace are in ${v} (JSON), and it woke you because there are some.`},assess:n=>{let r=C(n,`knip`);if(r===void 0)return;let{files:i,exports:a,types:o,dependencies:s,devDependencies:c,sample:l}=r.deadCode;if(i+a+o+s+c!==0)return{headline:`${S(i,`unreferenced file`)}, ${S(a+o,`unused export`)}, ${S(s+c,`unused dependency`,`unused dependencies`)}`,detail:l.map(e=>`unreferenced · ${e}`),digest:e(...l.toSorted(),`exports:${t(a+o)}`,`deps:${t(s+c)}`),severity:`info`,why:`knip reports ${S(i,`unreferenced file`)}, ${a+o} unused exports and ${s+c} unused dependencies in ${x(n.repo)}${l.length===0?``:`, among them ${l.join(`, `)}`}.`}},diagnosis:`Code nothing reaches still has to be read, type-checked and kept compiling by everyone who works nearby.`,goal:`Re-run knip yourself first — this measurement is hours old and the tree has moved. Then check each finding against how the file is actually used: knip is confidently wrong about anything reachable from OUTSIDE the repository, which means a package's public entry points, files a bundler or framework loads by convention, and types consumed only by a downstream package. Delete what is genuinely unreachable. Leave the false positives and list them in one line each, so the next run's reader knows they were considered rather than missed.`,done:`Done when knip reports fewer findings, the repository's type-check and tests pass, and nothing you deleted is reachable from another package.`},w=5,pe={id:`duplication`,title:`Find duplication worth collapsing`,icon:`clone`,description:`Copy-paste that has grown past a fifth of a percent of the tree. Reports only — extracting is a design call.`,kind:`drifting`,criterion:`jscpd reports more than 5% of the scanned tree duplicated.`,stance:`report`,needs:[`jscpd`],cadenceMs:30*g,automation:{cron:`0 3 * * 1`,guard:`pnpm dlx jscpd --reporters json --output ${y} --min-lines 12 --threshold 100 . >/dev/null 2>&1; [ "$(jq '.statistics.total.percentage // 0 | floor' ${b} 2>/dev/null || echo 0)" -ge ${w} ]`,note:`weekly · wakes above ${w}% duplication`,report:b,woke:`jscpd's clone report for this workspace is in ${b}, and it woke you because duplication is above ${w}%.`},assess:t=>{let n=C(t,`jscpd`);if(n===void 0||n.duplication.percentage<w)return;let{percentage:r,clones:i,top:a}=n.duplication;return{headline:`${r.toFixed(1)}% of the tree is duplicated, across ${S(i,`clone`)}`,detail:a.map(e=>`${e.lines} lines · ${e.first} ↔ ${e.second}`),digest:e(`pct:${Math.round(r)}`,...a.map(e=>`${e.first}|${e.second}`).toSorted()),severity:`info`,why:`jscpd reports ${r.toFixed(1)}% duplication across ${S(i,`clone`)} in ${x(t.repo)}; the largest are ${a.map(e=>`${e.first} ↔ ${e.second} (${e.lines} lines)`).join(`; `)}.`}},diagnosis:`Duplication only costs anything when the copies have to change together — and only some of it does.`,goal:`Report the clones where the copies genuinely have to change together. For each: cite both file:line ranges, say what the shared concept actually is, and name where the extraction would live. Then say explicitly which of the reported clones you are NOT recommending against — generated files, deliberately repetitive tests, and lookalikes owned by different subsystems — so the next reader knows the list was triaged rather than truncated.`,done:`Done when every clone in the report has either a named extraction or a one-line reason it should stay.`},me={id:`documentation-refresh`,title:`Document what nothing explains`,icon:`file-edit`,description:`Packages in this repository with no architecture document — new ones first.`,kind:`drifting`,criterion:`A workspace package has no docs/architecture document.`,applies:e=>e.packages.length>0?void 0:`not a workspace`,stance:`act`,needs:[],cadenceMs:90*g,assess:t=>{let n=t.signals.packages.filter(e=>!e.documented);if(n.length!==0)return{headline:`${S(n.length,`package`)} of ${t.signals.packages.length} have no document`,detail:n.map(e=>`${e.name} · ${e.dir}`),digest:e(...n.map(e=>e.dir).toSorted()),severity:`info`,why:`${S(n.length,`package`)} of ${t.signals.packages.length} in ${x(t.repo)} have no docs/architecture document: ${n.map(e=>e.dir).join(`, `)}.`}},diagnosis:`A package nobody can read the shape of gets worked in by guesswork, and the guesses accumulate.`,goal:`Follow this workspace's own documentation conventions — read them first, they are not optional and they are not generic. For each undocumented package, read the package before you write a word about it, and produce the document its conventions call for: what the package is FOR, how it fits the system, and which files matter. Explain at the module level. Never describe code line by line, and never document a package you did not read.`,done:`Done when every package you named has a document that a newcomer could use to find the file they need, and no other file changed.`},he=3,ge=e=>{if(e.length===0)return 0;let t=e.toSorted((e,t)=>e-t),n=Math.floor(t.length/2);return t.length%2==0?((t[n-1]??0)+(t[n]??0))/2:t[n]??0},_e={id:`complexity`,title:`Simplify what everything waits on`,icon:`wave-pulse`,description:`Files that both churn and carry the repository — where edits are slow and ripple outward.`,kind:`accruing`,criterion:`A file in the hotspot ranking is also a key module, or its branching is three times the median of that ranking.`,stance:`act`,needs:[],cadenceMs:30*g,assess:t=>{if(!t.signals.indexed||t.signals.hotspots.length===0)return;let n=new Set(t.signals.keyModules.map(e=>e.path)),r=ge(t.signals.hotspots.map(e=>e.complexity)),i=t.signals.hotspots.filter(e=>n.has(e.path)||e.complexity>=r*he);if(i.length===0)return;let a=(e,t)=>n.has(e)?`churns and the rest of the repository imports it`:`${t} branch points against a median of ${r}`;return{headline:`${S(i.length,`file`)} where every edit is slow and ripples outward`,detail:i.map(e=>`${e.path} — ${e.commits} commits, ${a(e.path,e.complexity)}`),digest:e(...i.map(e=>e.path).toSorted()),severity:`info`,why:`${S(i.length,`file`)} in ${x(t.repo)} are both change magnets and structurally tangled: ${i.map(e=>`${e.path} (${e.commits} commits, ${e.complexity} branch points)`).join(`; `)}.`}},diagnosis:`A file that changes constantly and branches heavily makes every edit near it slow and easy to get wrong.`,goal:`Take ONE file — the worst of them — and no more. Read it first. If the rest of the repository imports it, separate the stable contract from the churn: a narrow surface for importers, the volatile implementation private behind it. If it is simply tangled, flatten it where it stands — edge cases as early returns, compound conditions behind named predicates, long chains as lookups — and extract a unit only if a cohesive one falls out. Behaviour stays identical, and no re-export shims are left behind.`,done:"Done when `iq hotspots` reports materially fewer branch points for that file, the repository's checks pass, and no importer changed meaning."},ve={16:`2023-09-11`,18:`2025-04-30`,20:`2026-04-30`,22:`2027-04-30`,24:`2028-04-30`},ye=90*g,be={id:`runtime-eol`,title:`Move off an end-of-life runtime`,icon:`bolt`,description:`Whether the Node this sandbox runs still receives security patches.`,kind:`carrying`,criterion:`The Node release this sandbox runs is past its end-of-life date, or within 90 days of it.`,applies:e=>e.shape.packageManifest?void 0:`no package.json`,stance:`act`,needs:[],cadenceMs:0,assess:t=>{let n=Number.parseInt(t.node.replace(/^v/,``),10),r=ve[n];if(Number.isNaN(n)||r===void 0)return;let i=Date.parse(`${r}T00:00:00Z`);if(t.nowMs<i-ye)return;let a=t.nowMs>=i,o=Math.round(Math.abs(i-t.nowMs)/g),s=t.signals.packages.filter(e=>e.engines?.node!==void 0);return{headline:a?`Node ${n} stopped receiving security patches ${o} days ago`:`Node ${n} reaches end of life in ${o} days`,detail:[`running · ${t.node}`,`end of life · ${r}`,...s.map(e=>`pinned · ${e.name} requires node ${e.engines?.node??``}`)],digest:e(`node:${n}`,a?`eol`:`approaching`),severity:a?`warning`:`info`,why:`This sandbox runs ${t.node}, and Node ${n} ${a?`reached end of life on ${r}`:`reaches end of life on ${r}`} — ${S(s.length,`package`)} in ${x(t.repo)} pin a node engine range.`}},diagnosis:`An unsupported runtime stops receiving security patches, so every advisory against it stays open permanently.`,goal:`Establish what actually pins this runtime: the image's own base, the workspace's useNodeVersion, and each package's engines range. Propose the smallest move to a supported LTS — which of those pins have to change, in what order, and what is likely to break at that boundary. Make the pin changes that are mechanical; do NOT attempt the image rebuild itself.`,done:`Done when the pins name a supported release, the repository's type-check and tests pass on it, and anything needing a rebuild is named as such.`},T=[{category:`date handling`,members:[`moment`,`dayjs`,`date-fns`,`luxon`,`js-joda`]},{category:`HTTP clients`,members:[`axios`,`got`,`node-fetch`,`superagent`,`undici`,`request`]},{category:`schema validation`,members:[`zod`,`yup`,`joi`,`ajv`,`superstruct`,`valibot`]},{category:`utility belts`,members:[`lodash`,`underscore`,`ramda`,`remeda`]},{category:`state stores`,members:[`redux`,`mobx`,`zustand`,`jotai`,`recoil`,`pinia`,`valtio`]},{category:`UUID generation`,members:[`uuid`,`nanoid`,`cuid`,`shortid`,`ulid`]},{category:`test runners`,members:[`jest`,`mocha`,`ava`,`tap`]}],E={id:`library-overlap`,title:`Settle on one library per job`,icon:`box`,description:`Two dependencies solving the same problem — both shipped, both maintained, one picked at random.`,kind:`drifting`,criterion:`Two or more installed dependencies do the same job.`,applies:e=>e.packages.length>0?void 0:`not a workspace`,stance:`report`,needs:[],cadenceMs:90*g,assess:t=>{let n=new Set(t.signals.packages.flatMap(e=>[...e.dependencies,...e.devDependencies])),r=T.map(({category:e,members:t})=>({category:e,found:t.filter(e=>n.has(e))})).filter(({found:e})=>e.length>1);if(r.length!==0)return{headline:`${S(r.length,`job`)} done by more than one library`,detail:r.map(({category:e,found:t})=>`${e} · ${t.join(`, `)}`),digest:e(...r.map(({category:e,found:t})=>`${e}:${t.toSorted().join(`+`)}`).toSorted()),severity:`info`,why:`${x(t.repo)} depends on more than one library for the same job: ${r.map(({category:e,found:t})=>`${e} (${t.join(`, `)})`).join(`; `)}.`}},diagnosis:`Two libraries for one job means both ship, both need upgrading, and new code picks whichever the neighbouring file used.`,goal:`For each overlapping pair, find out which one is actually load-bearing — how many call sites each has, whether one is a transitive dependency nobody chose, and whether either is unmaintained. Recommend the one to keep and estimate the migration honestly, including the call sites where the two libraries genuinely differ in behaviour. Where the overlap is deliberate or the second is only transitive, say so and close the question.`,done:`Done when every overlapping pair has a recommendation with a call-site count behind it, or a reason the overlap is fine.`},D=8,O=s.map(e=>e.label).join(`, `),k=e=>l(e.shape.deps).length>0?void 0:`no ${O}`,A=e=>e>=1048576?`${(e/1048576).toFixed(1)} MB`:`${Math.round(e/1024)} kB`,j=50,M=3,N=e=>e.replace(/[.-](?=[A-Za-z0-9_-]*[0-9])[A-Za-z0-9_-]{8,}(\.[a-z0-9]+)$/,`$1`),xe={id:`bundle-weight`,title:`Split what the browser downloads first`,icon:`download`,description:`What the last build put on disk, and whether it arrives as one download or several.`,kind:`accruing`,criterion:`A single asset is more than half of the build's total transfer size.`,applies:k,stance:`report`,needs:[`bundle`],cadenceMs:30*g,assess:n=>{let r=C(n,`bundle`);if(r===void 0)return;let{assets:i,totalGzip:a,dir:o}=r.bundle;if(i.length<M||a===0)return;let s=i.toSorted((e,t)=>t.gzip-e.gzip),c=s[0];if(c===void 0)return;let l=c.gzip/a*100;if(!(l<j))return{headline:`${c.path} is ${Math.round(l)}% of the ${A(a)} this build ships`,detail:s.slice(0,D).map(e=>`${A(e.gzip)} gzipped · ${e.path} (${A(e.bytes)} on disk)`),digest:e(`total:${t(a)}`,...s.slice(0,5).map(e=>N(e.path)).toSorted()),severity:`info`,why:`The build output in ${o}/ of ${x(n.repo)} is ${A(a)} gzipped across ${S(i.length,`asset`)}, and ${c.path} alone is ${A(c.gzip)} of it — ${Math.round(l)}%. The next largest are ${s.slice(1,4).map(e=>`${e.path} (${A(e.gzip)})`).join(`, `)}. This is the last build someone ran, read off disk; nothing rebuilt it to measure.`}},diagnosis:`Everything in the first chunk is downloaded and parsed before anything renders, whether or not the visitor needed it.`,goal:`Find out what is actually IN the dominant chunk before proposing anything — the repository's own bundler can report this, and a recommendation made without it is guesswork. Then report the split worth making: which routes or features could load on demand, which dependencies are pulled in wholesale for one function, and which are only used behind an interaction nobody has yet had. Name the boundary for each and estimate what it saves. Where the chunk is genuinely all first-paint code, say so and close it.`,done:`Done when every recommendation names a specific import boundary and the bytes it would move out of the first download.`},Se={id:`framework-idiom`,title:`Finish the framework migrations`,icon:`history`,description:`Code still written the way the framework used to recommend, long after it stopped.`,kind:`accruing`,criterion:`A file uses a framework idiom that framework's own maintainers have replaced.`,applies:k,stance:`act`,needs:[`ui`],cadenceMs:60*g,assess:n=>{let r=C(n,`ui`);if(r===void 0)return;let i=new Set(l(n.signals.shape.deps).map(e=>e.id)),a=r.scan.idioms.flatMap(({id:e,files:t})=>{let n=ne(e);return n===void 0||!i.has(n.framework)||t.length===0?[]:[{rule:n,files:t}]});if(a.length===0)return;let o=a.reduce((e,t)=>e+t.files.length,0),s=a.toSorted((e,t)=>t.files.length-e.files.length);return{headline:`${S(a.length,`retired idiom`)} still in use, across ${S(o,`file`)}`,detail:s.map(e=>`${S(e.files.length,`file`)} · ${e.rule.label} → ${e.rule.replacement}`),digest:e(...s.map(e=>`${e.rule.id}:${t(e.files.length)}`).toSorted()),severity:`info`,why:`${x(n.repo)} still uses ${S(a.length,`idiom`)} its framework has replaced: ${s.map(e=>`${e.rule.label} in ${S(e.files.length,`file`)} (replaced by ${e.rule.replacement})`).join(`; `)}. A sample of the files: ${s.flatMap(e=>e.files.slice(0,3)).slice(0,D).join(`, `)}.`}},diagnosis:`A retired idiom keeps working until the major release that drops it, and then it is an emergency inside somebody else's upgrade.`,goal:`Take ONE idiom, the one with the most files, and no more. Convert the files where the conversion is mechanical and the behaviour is provably identical. Stop at the first file that needs a design decision — a class component with genuine error-boundary semantics, an NgModule that something outside the repository imports — leave it, and say what it would take. Do not convert an idiom the repository has deliberately kept: if the newest code uses it too, that is a choice, and reporting it as one is the useful answer.`,done:`Done when a re-scan reports fewer files on that idiom, the repository's type-check and tests pass, and every file you skipped has a one-line reason.`},Ce={id:`component-overlap`,title:`Settle on one component per job`,icon:`copy`,description:`Components built twice — the same name in two places, or the same logic under two names.`,kind:`drifting`,criterion:`Two component files reduce to the same name, or a duplicated block spans two components.`,applies:k,stance:`report`,needs:[`ui`,`jscpd`],cadenceMs:90*g,assess:t=>{let n=C(t,`ui`),r=C(t,`jscpd`);if(n===void 0||r===void 0)return;let i=new Map;for(let e of n.scan.components){let t=ae(e);t!==void 0&&i.set(t,[...i.get(t)??[],m(e)])}let a=[...i].filter(([,e])=>e.length>1).map(([e,t])=>({stem:e,paths:t.toSorted()})).toSorted((e,t)=>t.paths.length-e.paths.length),o=new Set(n.scan.components.map(m)),s=r.duplication.top.filter(e=>o.has(m(e.first))&&o.has(m(e.second)));if(a.length===0&&s.length===0)return;let c=[...a.length===0?[]:[`${S(a.length,`name`)} used by more than one component`],...s.length===0?[]:[`${S(s.length,`clone`)} spanning two of them`]];return{headline:c.join(`, `),detail:[...a.slice(0,D).map(e=>`${e.stem} · ${e.paths.join(`, `)}`),...s.map(e=>`${e.lines} shared lines · ${m(e.first)} ↔ ${m(e.second)}`)],digest:e(...a.map(e=>`${e.stem}:${e.paths.join(`+`)}`).toSorted(),...s.map(e=>`${m(e.first)}|${m(e.second)}`).toSorted()),severity:`info`,why:`${x(t.repo)} has ${c.join(` and `)}, out of ${S(n.scan.components.length,`component file`)} scanned. ${a.length===0?``:`The names: ${a.slice(0,D).map(e=>`${e.stem} (${e.paths.join(`, `)})`).join(`; `)}. `}${s.length===0?``:`The clones: ${s.map(e=>`${m(e.first)} ↔ ${m(e.second)}, ${e.lines} lines`).join(`; `)}.`}`}},diagnosis:`A component built twice is maintained once — whichever copy the next person happens to open is the one that gets the fix.`,goal:`Read every file in each group before saying anything about it; a shared name is a reason to look, not a finding on its own. For each group, say whether these genuinely do the same job, and if they do, name the one to keep and count the call sites that would have to move. Where the answer is that the same LOGIC is duplicated rather than the whole component — the same fetch and loading state, the same form validation, the same list virtualization written twice — say so, and name the hook or composable it should become and where it would live. Where two components share a name and nothing else, say that too and close it: a false family is worth one line, and the next reader needs to know it was considered.`,done:`Done when every group has either a component to keep with a call-site count, a shared unit to extract with a home, or a reason it is fine.`},we={id:`tailwind-arbitrary-values`,title:`Put hard-coded styles back on the scale`,icon:`palette`,description:`Colours and sizes written inline in the markup, around the theme that already defines them.`,kind:`drifting`,criterion:`A Tailwind class hard-codes a colour or a pixel size instead of using the theme's scale.`,applies:e=>u(e.shape.deps)?void 0:`no Tailwind`,stance:`act`,needs:[`ui`],cadenceMs:30*g,assess:n=>{let r=C(n,`ui`);if(r===void 0)return;let{bypasses:i}=r.scan;if(i.length===0)return;let a=i.reduce((e,t)=>e+t.count,0),o=i.toSorted((e,t)=>t.count-e.count).slice(0,D);return{headline:`${S(a,`hard-coded value`)} across ${S(i.length,`file`)}`,detail:o.map(e=>`${e.path} · ${S(e.count,`value`)}`),digest:e(...o.map(e=>e.path).toSorted(),`files:${t(i.length)}`,`total:${t(a)}`),severity:`info`,why:`${x(n.repo)} has ${S(a,`Tailwind class`,`Tailwind classes`)} hard-coding a colour or a pixel size across ${S(i.length,`file`)}; the heaviest are ${o.slice(0,5).map(e=>`${e.path} (${e.count})`).join(`, `)}.`}},diagnosis:`Every inline colour is a place the theme cannot reach — a palette change lands everywhere except the files that opted out of it.`,goal:`Read the theme first — the Tailwind config, or the CSS that defines the tokens — so you know what the scale actually offers. Then replace the values that have a token: an exact palette match, a spacing step, a type size. Where a value is CLOSE to a token but not equal, do not round it silently; that is a visual change wearing a refactor's clothes. List those separately with both values and let the owner decide. Where a value has no token and should — a brand colour used in nine places — say that the theme is missing an entry rather than editing nine files.`,done:`Done when a re-scan reports fewer hard-coded values, nothing renders differently, and every value you left has a one-line reason.`},P=({id:t,title:n,icon:r,description:i,diagnosis:a,goal:o,done:s,cadenceDays:c,applies:l})=>({id:t,title:n,icon:r,description:i,kind:`surveying`,criterion:`${c} days have passed since this review was last run.`,applies:l,stance:`report`,needs:[],cadenceMs:c*g,survey:!0,assess:n=>({headline:`Not surveyed in ${c} days`,detail:[`Cadence · every ${c} days`],digest:e(t,`period:${Math.floor(n.nowMs/(c*g))}`),severity:`info`,why:`This is a periodic review of ${x(n.repo)}, run every ${c} days; nothing measured it — it is due because it has been that long.`}),diagnosis:a,goal:o,done:s}),Te=25,Ee=P({id:`standardize-patterns`,title:`Standardize the cross-cutting patterns`,icon:`sitemap`,description:`Error handling, validation, logging, configuration, retries, pagination — the things every file does slightly differently.`,diagnosis:`Cross-cutting concerns drift one file at a time, and the cost only shows up when someone has to work across several of them.`,goal:`Pick the cross-cutting concerns this repository actually has — error handling, input validation, logging, configuration, retries, pagination, serialization — and for each, survey how it is done. Name the dominant pattern, the outliers, and which of the outliers are deliberate. Recommend ONE convention per concern with a file to point at as the reference implementation, and estimate the size of the conversion. Do not convert anything.`,done:`Done when each concern has a named convention, a reference file, and a count of the sites that diverge from it.`,cadenceDays:90,applies:e=>e.totals.files>=Te?void 0:`only ${e.totals.files} indexed files`}),De=P({id:`deprecated-apis`,title:`Audit deprecated APIs`,icon:`exclamation-triangle`,description:`Language, runtime and framework APIs this code still uses that their own maintainers have moved on from.`,diagnosis:`A deprecated API works right up until the upgrade that removes it, and then it is an emergency during someone else's migration.`,goal:`Survey what this repository uses that its own dependencies have deprecated: read the framework and runtime versions in use, check their deprecation notices, and search for the call sites. Include the repository's OWN deprecations — anything its code marks as deprecated and still calls. Rank by when each one actually breaks, not by how many call sites it has, and name the replacement for each. Change nothing.`,done:`Done when every deprecation has call sites cited, a replacement named, and the release it is expected to break in.`,cadenceDays:90,applies:e=>e.shape.packageManifest?void 0:`no package.json`}),Oe=P({id:`documentation-drift`,title:`Re-read the documentation against the code`,icon:`file`,description:`Whether what the documents claim is still what the code does — the drift no tool can measure.`,diagnosis:`Documentation is trusted in proportion to how recently it was true, and a document that is quietly wrong is worse than a missing one.`,goal:`Read this repository's architecture documents against the code they describe. Report every claim that is no longer true, citing the document line and the file that contradicts it. Prioritise the claims someone would ACT on — where a subsystem lives, what owns what, which file to change — over prose that has merely aged. Do not rewrite the documents; produce the list of what is wrong.`,done:`Done when every architecture document has been read and every false claim is listed with both sides cited.`,cadenceDays:90,applies:e=>e.shape.docs.length>0?void 0:`no architecture documents`}),ke=P({id:`ci-hygiene`,title:`Tighten the CI pipeline`,icon:`bolt`,description:`What the pipeline re-does every run: uncached installs, rebuilt layers, jobs that could run in parallel.`,diagnosis:`A slow pipeline is paid on every push by everyone, and it degrades one uncached step at a time without anyone deciding to.`,goal:`Read this repository's pipeline definitions and report what it pays for repeatedly: dependency installs with no cache key, build outputs recomputed between jobs, steps that are serial for no reason, and matrix legs that duplicate each other's work. For each, name the file and step, say roughly what it costs per run, and give the change that would fix it. Where a step is slow because it genuinely has to be, say so — a pipeline that is honestly expensive is not a finding.`,done:`Done when every finding names a file, a step, and a concrete change, and anything deliberately slow is called out as such.`,cadenceDays:90,applies:e=>e.shape.ci.length>0?void 0:`no CI pipeline`}),Ae=P({id:`docker-image`,title:`Slim the container image`,icon:`box`,description:`Layer order, build context and final size — what ships in the image that did not need to.`,diagnosis:`Image size is paid on every pull and every cold start, and layer order decides how much of a build is cache hits.`,goal:`Read this repository's Dockerfiles and report what makes the image larger or the build slower than it needs to be: layers ordered so that a source edit invalidates the dependency install, build-time toolchains left in the final stage, a build context that ships the whole repository, and package caches never cleaned. For each, cite the file and line, and name the change. Do not rewrite the Dockerfiles — an image that fails to build is a much worse problem than one that is larger than ideal.`,done:`Done when every finding cites a Dockerfile line and names the change, with the ones that would need a base-image swap called out separately.`,cadenceDays:90,applies:e=>e.shape.dockerfiles.length>0?void 0:`no Dockerfile`}),F=[{kind:`carrying`,label:`Carrying`,caption:`a risk this repository is running today — someone else decides when it becomes urgent`},{kind:`accruing`,label:`Accruing`,caption:`cheap now, expensive later, and always getting later`},{kind:`drifting`,label:`Drifting`,caption:`the shape of the thing is diverging from the idea of it`},{kind:`surveying`,label:`Surveying`,caption:`periodic reads with nothing measuring them — due because it has been that long`}],je=[le,be,de,fe,_e,xe,Se,me,pe,E,Ce,we,Ee,De,Oe,ke,Ae],I=F.map(({kind:e})=>e),L=je.toSorted((e,t)=>I.indexOf(e.kind)-I.indexOf(t.kind)),Me=e=>e.automation===void 0?void 0:n({subject:`${e.title} across this workspace.`,why:`${e.automation.woke} ${r}`,diagnosis:e.diagnosis,goal:e.goal,invariants:e.stance===`act`?i:a,done:e.done}),Ne=(e,t,o)=>n({subject:`${e.title} in ${x(o)}.`,why:`${t.why} You were woken because: ${e.criterion} ${r}`,diagnosis:e.diagnosis,goal:e.goal,invariants:e.stance===`act`?i:a,done:e.done}),R=864e5,z=e=>{let t=e.indexOf(`{`);if(t!==-1)try{let n=JSON.parse(e.slice(t));return typeof n==`object`&&n&&!Array.isArray(n)?n:void 0}catch{return}},B=e=>typeof e==`string`&&e!==``?e:void 0,V=e=>Array.isArray(e)?e.length:0,H=e=>e.replace(/^[^\d]*/,``).split(`.`).map(e=>Number.parseInt(e,10)||0),Pe=(e,t)=>{let[n=0,r=0]=H(e),[i=0,a=0]=H(t);return i===n?a===r?`patch`:`minor`:`major`},Fe=e=>{let t=z(e);if(t===void 0)return;let n=[];for(let[e,r]of Object.entries(t)){if(typeof r!=`object`||!r)continue;let t=r,i=B(t.current),a=B(t.latest);i!==void 0&&a!==void 0&&i!==a&&n.push({name:e,current:i,latest:a,kind:Pe(i,a),section:B(t.dependencyType)??`dependencies`})}return{id:`outdated`,packages:n}},Ie=new Set([`critical`,`high`,`moderate`,`low`,`info`]),Le=e=>{let t=z(e);if(t===void 0)return;let n=t.advisories;if(n===void 0)return{id:`audit`,advisories:[]};if(typeof n!=`object`||!n)return;let r=[];for(let e of Object.values(n)){if(typeof e!=`object`||!e)continue;let t=e,n=B(t.module_name),i=B(t.severity);if(n===void 0||i===void 0||!Ie.has(i))continue;let a=B(t.patched_versions),o=Array.isArray(t.findings)?t.findings:[];r.push({name:n,severity:i,title:B(t.title)??n,...a===void 0||a===`<0.0.0`?{}:{patched:a},dev:o.length>0&&o.every(e=>e.dev===!0)})}return{id:`audit`,advisories:r}},Re=8,ze=e=>{let t=z(e)?.issues;if(!Array.isArray(t))return;let n=t.filter(e=>typeof e==`object`&&!!e),r=e=>n.reduce((t,n)=>t+V(n[e]),0);return{id:`knip`,deadCode:{files:r(`files`),exports:r(`exports`),types:r(`types`),dependencies:r(`dependencies`),devDependencies:r(`devDependencies`),sample:n.flatMap(e=>V(e.files)===0?[]:B(e.file)??[]).slice(0,Re)}}},Be=5,Ve=e=>{let t=z(e),n=t?.statistics;if(typeof n!=`object`||!n)return;let r=n.total,i=typeof r==`object`&&r?r.percentage:void 0,a=Array.isArray(t?.duplicates)?t.duplicates:[],o=e=>typeof e==`object`&&e?B(e.name)??`?`:`?`;return{id:`jscpd`,duplication:{percentage:typeof i==`number`?i:0,clones:a.length,top:a.map(e=>({lines:typeof e.lines==`number`?e.lines:0,first:o(e.firstFile),second:o(e.secondFile)})).toSorted((e,t)=>t.lines-e.lines).slice(0,Be)}}},U=`UI`,He=2e3,W=500,G=`.`,K=e=>[...e,...ee].map(e=>`-g '${e}'`).join(` `),Ue=e=>{let t=e.lastIndexOf(`:`);if(t<=0)return;let n=Number.parseInt(e.slice(t+1),10);return Number.isNaN(n)||n<=0?void 0:{path:m(e.slice(0,t)),count:n}},We=e=>`rg --no-messages ${e.absent===void 0?`-l`:`--files-without-match`} -e '${e.pattern}' ${K(e.globs)} ${G} 2>/dev/null | sort | head -n ${W} | awk '{print "IDIOM\\t${e.id}\\t" $0}'`,Ge=()=>[`echo ${U}`,`rg --files ${K(te)} ${G} 2>/dev/null | sort | head -n ${He} | awk '{print "COMPONENT\\t" $0}'`,`rg --no-messages --count-matches -e '${f}' ${K(d)} ${G} 2>/dev/null | sort | head -n ${W} | awk '{print "BYPASS\\t" $0}'`,...p.map(We),`true`].join(`; `),Ke=e=>{let t=e.split(`
`).map(e=>e.trim());if(t.find(e=>e!==``)!==U)return;let n=[],r=[],i=new Map;for(let e of t){let[t,...a]=e.split(`	`);if(t===`COMPONENT`&&a[0]!==void 0)n.push(m(a[0]));else if(t===`BYPASS`){let e=Ue(a.join(`	`));e!==void 0&&r.push(e)}else if(t===`IDIOM`&&a[0]!==void 0){let e=m(a.slice(1).join(`	`));e!==``&&i.set(a[0],[...i.get(a[0])??[],e])}}return{id:`ui`,scan:{components:n,bypasses:r,idioms:[...i].map(([e,t])=>({id:e,files:t}))}}},q=[`dist`,`build`,`out`,`public/build`],J=`DIR`,qe=40,Je=()=>[`dir=""`,`for d in ${q.join(` `)}; do if [ -d "$d" ]; then dir="$d"; break; fi; done`,`[ -n "$dir" ] || exit 0`,`printf '${J}\\t%s\\n' "$dir"`,`find "\$dir" -type f \\( -name '*.js' -o -name '*.mjs' -o -name '*.cjs' -o -name '*.css' \\) -exec sh -c 'for f; do printf "ASSET\\t%s\\t%s\\t%s\\n" "\$(wc -c <"\$f")" "\$(gzip -c "\$f" | wc -c)" "\$f"; done' _ {} + 2>/dev/null | sort -k2 -rn | head -n ${qe}`].join(`; `),Y=e=>{let t=e.split(`
`).map(e=>e.trim()),n=t.find(e=>e.startsWith(`${J}\t`));if(n===void 0)return;let r=[];for(let e of t){let[t,n,i,...a]=e.split(`	`);if(t!==`ASSET`||a.length===0)continue;let o=Number.parseInt(n??``,10),s=Number.parseInt(i??``,10);Number.isNaN(o)||Number.isNaN(s)||r.push({path:a.join(`	`),bytes:o,gzip:s})}return{id:`bundle`,bundle:{dir:n.slice(J.length+1),totalBytes:r.reduce((e,t)=>e+t.bytes,0),totalGzip:r.reduce((e,t)=>e+t.gzip,0),assets:r}}},X=`/tmp/intentic-chore-jscpd`,Ye=[{id:`outdated`,title:`Dependency versions`,measures:`how far behind the registry each dependency is`,tier:1,ttlMs:R,timeoutMs:3e5,available:`test -f package.json`,unavailable:`no package.json`,command:`pnpm outdated -r --json 2>/dev/null || true`,parse:Fe},{id:`audit`,title:`Security advisories`,measures:`published advisories against this dependency tree`,tier:1,ttlMs:R,timeoutMs:3e5,available:`test -f pnpm-lock.yaml || test -f package-lock.json`,unavailable:`no lockfile`,command:`pnpm audit --json 2>/dev/null || true`,parse:Le},{id:`knip`,title:`Unreachable code`,measures:`files, exports and dependencies nothing references`,tier:2,ttlMs:7*R,timeoutMs:9e5,available:`pnpm exec knip --version >/dev/null 2>&1`,unavailable:`knip is not a devDependency`,command:`pnpm exec knip --reporter json --no-exit-code 2>/dev/null || true`,parse:ze},{id:`jscpd`,title:`Copy-paste`,measures:`how much of the tree is duplicated elsewhere in it`,tier:2,ttlMs:7*R,timeoutMs:12e5,available:`test -f package.json`,unavailable:`no package.json`,command:`pnpm dlx jscpd --reporters json --output ${X} --min-lines 12 --threshold 100 . >/dev/null 2>&1; cat ${X}/jscpd-report.json 2>/dev/null`,parse:Ve},{id:`ui`,title:`Front-end source`,measures:`components, hard-coded styles and idioms the framework has replaced`,tier:1,ttlMs:R,timeoutMs:3e5,available:`rg -l --no-messages -g '**/package.json' -g '!**/node_modules/**' -e '[\\x22](${[...s.flatMap(e=>e.packages),...c].join(`|`)})[\\x22]\\s*:' . >/dev/null`,unavailable:`no package here declares a UI framework or Tailwind`,command:Ge(),parse:Ke},{id:`bundle`,title:`Build output`,measures:`what the last build put on disk for a browser to download`,tier:1,ttlMs:R,timeoutMs:3e5,available:`find ${q.join(` `)} -maxdepth 4 -type f \\( -name '*.js' -o -name '*.mjs' -o -name '*.css' \\) 2>/dev/null | head -n 1 | grep -q .`,unavailable:`no build output on disk — this reads the last build, it never runs one`,command:Je(),parse:Y}],Z=e=>{let t=Ye.find(t=>t.id===e);if(t===void 0)throw Error(`chores: no probe named "${e}"`);return t},Q=(e,t,n)=>e.survey===!0&&t!==void 0?`Surveyed ${Math.round((n-t.ranAt)/864e5)} days ago`:`Nothing to do`,Xe=(e,t)=>e.flatMap(e=>{let n=t.get(e),r=Z(e);return n===void 0?[`${r.title} · not measured yet`]:n.state===`ok`?[]:n.state===`unavailable`?[`${r.title} · ${n.reason??`not available in this repository`}`]:[`${r.title} · failed${n.reason===void 0?``:` — ${n.reason}`}`]}),Ze=(e,t,n)=>{let r={chore:e,repo:t.repo,lastRun:n,settled:!1,prompt:void 0},i=e.applies?.(t.signals);if(i!==void 0)return{...r,state:`not-applicable`,severity:`info`,headline:i,detail:[],digest:``};let a=Xe(e.needs,t.probes);if(a.length>0)return{...r,state:`unavailable`,severity:`info`,headline:`Not measured`,detail:a,digest:``};let o=e.assess(t);if(o===void 0)return{...r,state:`clear`,severity:`info`,headline:Q(e,n,t.nowMs),detail:[],digest:``};let s=n!==void 0&&e.cadenceMs>0&&t.nowMs-n.ranAt>=e.cadenceMs,c=n?.digest===o.digest&&!s;if(e.survey===!0&&n!==void 0&&t.nowMs-n.ranAt<e.cadenceMs)return{...r,state:`clear`,severity:`info`,headline:Q(e,n,t.nowMs),detail:o.detail,digest:o.digest};let l=Ne(e,o,t.repo);return n?.snoozedUntil!==void 0&&n.snoozedUntil>t.nowMs?{...r,state:`snoozed`,severity:`info`,headline:o.headline,detail:o.detail,digest:o.digest,prompt:l}:c&&n?.outcome===`clean`?{...r,state:`clear`,severity:`info`,headline:`Checked — the findings did not hold up`,detail:o.detail,digest:o.digest}:{...r,state:`due`,severity:o.severity,headline:o.headline,detail:o.detail,digest:o.digest,prompt:l,settled:c}},$=(e,t)=>`${e}|${t}`,Qe=(e,t)=>{let n=new Map(e.ledger.map(e=>[$(e.repo,e.chore),e]));return e.repos.flatMap(({repo:r,probes:i,signals:a})=>{let o={repo:r,probes:new Map(i.map(e=>[e.id,e])),signals:a,node:e.node,nowMs:t};return L.map(e=>Ze(e,o,n.get($(r,e.id))))})},$e=(e,t)=>e.filter(e=>e.state===`due`&&!e.settled&&t[$(e.repo,e.chore.id)]!==e.digest);export{L as a,x as c,n as d,e as f,Z as i,oe as l,$ as n,F as o,$e as r,Me as s,Qe as t,o as u};