// The agent build, reported in the hello frame and surfaced per machine on the sandbox's card — so a computer
// running an old binary is visible there rather than mysteriously missing a tool. A literal, not a package.json
// read: the shipped artifact is a single compiled binary with no package.json beside it to read.
export const HOST_VERSION = "0.1.0";
