<script setup lang="ts">
import AgentsView from "../agents/AgentsView.vue";

/* The Agents area (/agents): the fleet of parallel isolated conversations — the coworking-space home surface
 * on both form factors (mobile lands here). Thin route wrapper, mirroring Chat.vue's shape. */
</script>

<template>
    <AgentsView class="h-full" />
</template>
