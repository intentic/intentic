# Root + nested packages.
**/node_modules
**/.cache
**/.local
**/.turbo
**/dist
**/dist-*
**/deploy
**/generated
**/out-tsc
**/.astro
**/.wrangler
**/*.tsbuildinfo
**/npm-debug.log*
**/pnpm-debug.log*

# desktop (Tauri): the Rust build tree, tauri-build's codegen, and installers staged into the site for a
# local download test (_apps/desktop/scripts/stage-local-downloads.sh).
_apps/desktop/src-tauri/target
_apps/desktop/src-tauri/gen
_apps/site/public/desktop
/tmp
**/tmp
**/.astro-indexnow-cache.json

# The interactive demo builds into the marketing site's public/ (see _apps/demo/vite.config.ts), which Astro
# copies into its dist — a build artifact living inside a tracked directory, so it is named rather than covered
# by the dist globs above.
_apps/site/public/demo

# Benchmark output — every iq-bench run writes a timestamped dir here; the datasets are the input worth keeping.
_tools/iq-bench/results

# env / secrets
**/.env
**/.env.local
**/.env.*.local
**/.env.production

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Claude Code per-user local settings — never shared across devs.
/.claude/settings.local.json

# intentic dogfood / demo (local only)
/.intentic
/intent
/desired-state
/app
/.demo
.image-out
