// Names of the starter site seeded on first boot (scaffold/starter-site.ts), shared so the browser can find what the
// daemon started. site is the repo dir; landing is the app name, also in the preview hostname
// (preview-site--landing-<id>.<zone>) and its _apps/ folder. Renaming breaks already-seeded workspaces.
export const STARTER_REPO = "site";
export const STARTER_APP = "landing";
