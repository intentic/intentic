# Account

The hosted platform plane: sign-in, billing, the web↔api contract, the database schema, and the capability
catalog. Deliberately off the command path — the editor talks to the sandbox directly, and this plane cannot
reach into anyone's box.
