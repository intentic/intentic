{
    "name": "@intentic/ingress",
    "version": "0.0.0",
    "private": true,
    "description": "The edge every sandbox is reached through: a Rust crate; this manifest only gives turbo its release task",
    "scripts": {
        "docker:release": "bash ../../_tools/scripts/platform/docker-release.sh ingress --build-context tunnel=../../_sandbox/front/crates/tunnel --build-context browser-wire=../../_sandbox/front/crates/browser-wire --build-context relay=../../_shared/relay"
    }
}
