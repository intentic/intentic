# Code search

How an agent finds the right file instead of reading everything: the `iq` CLI, the engine behind it, the
session-recall index, the LSP sidecar, and the benchmark that keeps retrieval honest.
